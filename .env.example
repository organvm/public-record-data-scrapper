# UCC-MCA Intelligence Platform - Environment Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# FEATURE FLAGS
# =============================================================================

# Use mock data (set to false for production with real data)
VITE_USE_MOCK_DATA=true

# Enable real-time data ingestion
VITE_ENABLE_REALTIME_INGESTION=false

# Enable ML-based enrichment
VITE_ENABLE_ML_ENRICHMENT=false

# Enable automatic data refresh
VITE_ENABLE_AUTO_REFRESH=false

# Enable debug mode (additional logging)
VITE_DEBUG_MODE=false

# =============================================================================
# DATA SOURCES
# =============================================================================

# UCC Filing API Configuration
# Provider: Your UCC filing data provider
VITE_UCC_API_ENDPOINT=https://api.ucc-filings.com/v1
VITE_UCC_API_KEY=your_ucc_api_key_here

# Alternative Data Sources (optional)
# Add additional endpoints as needed
# VITE_UCC_API_ENDPOINT_2=https://api.alternative-provider.com/v1
# VITE_UCC_API_KEY_2=your_alternative_api_key

# =============================================================================
# STATE UCC SCRAPER AUTHENTICATION
# =============================================================================

# Texas SOS Portal Credentials (Required as of September 2025)
# Sign up at: https://www.sos.state.tx.us/ucc/
# These credentials enable automated UCC searches in Texas
TX_UCC_USERNAME=your_texas_sos_username
TX_UCC_PASSWORD=your_texas_sos_password
# TX_UCC_MFA_SECRET=your_totp_secret  # Optional: for 2FA (future use)

# Florida UCC Credentials (Optional - not currently required)
# Florida UCC searches are available without authentication at floridaucc.com
# FL_UCC_USERNAME=your_florida_username
# FL_UCC_PASSWORD=your_florida_password

# California UCC Credentials (Optional - free searches available)
# Basic searches work without login at bizfileonline.sos.ca.gov
# CA_UCC_USERNAME=your_california_username
# CA_UCC_PASSWORD=your_california_password

# =============================================================================
# ML & ENRICHMENT SERVICES
# =============================================================================

# Machine Learning API
# For revenue estimation and industry classification
VITE_ML_API_ENDPOINT=https://ml.example.com/v1
VITE_ML_API_KEY=your_ml_api_key_here

# Web Scraper Service
# For growth signals and health score detection
VITE_SCRAPER_ENDPOINT=https://scraper.example.com
VITE_SCRAPER_API_KEY=your_scraper_api_key

# =============================================================================
# EXTERNAL DATA PROVIDERS (Optional)
# =============================================================================

# Job Boards (for hiring signals)
# VITE_INDEED_API_KEY=your_indeed_api_key
# VITE_LINKEDIN_API_KEY=your_linkedin_api_key

# Business Intelligence
# VITE_DNB_API_KEY=your_dnb_api_key
# VITE_CLEARBIT_API_KEY=your_clearbit_api_key

# Government Contracts (for contract signals)
# VITE_USASPENDING_API_KEY=your_usaspending_api_key

# Reviews & Sentiment (for health scores)
# VITE_YELP_API_KEY=your_yelp_api_key
# VITE_GOOGLE_PLACES_API_KEY=your_google_places_api_key

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL Database Connection
# Note: These are NOT prefixed with VITE_ as they are used server-side only
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ucc_mca
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# Database Pool Configuration (optional)
DB_POOL_MAX=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# Enable query logging (for debugging)
DB_LOG_QUERIES=false

# Redis for caching (optional)
# REDIS_URL=redis://localhost:6379

# =============================================================================
# SCHEDULER CONFIGURATION
# =============================================================================

# Ingestion interval (milliseconds) - default: 24 hours
# VITE_INGESTION_INTERVAL=86400000

# Enrichment interval (milliseconds) - default: 6 hours
# VITE_ENRICHMENT_INTERVAL=21600000

# Refresh interval (milliseconds) - default: 12 hours
# VITE_REFRESH_INTERVAL=43200000

# Stale data threshold (days) - default: 7 days
# VITE_STALE_DATA_THRESHOLD=7

# =============================================================================
# RATE LIMITING
# =============================================================================

# API rate limits (requests per minute)
# VITE_API_RATE_LIMIT=100
# VITE_SCRAPER_RATE_LIMIT=30

# =============================================================================
# SECURITY
# =============================================================================

# Encryption key for sensitive data (generate with: openssl rand -base64 32)
# VITE_ENCRYPTION_KEY=your_32_byte_base64_encoded_key

# CORS origins (comma-separated)
# VITE_CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# =============================================================================
# MONITORING & ANALYTICS
# =============================================================================

# Sentry DSN for error tracking
# VITE_SENTRY_DSN=your_sentry_dsn

# Google Analytics ID
# VITE_GA_ID=your_ga_id

# PostHog API Key for product analytics
# VITE_POSTHOG_KEY=your_posthog_key

# =============================================================================
# DEVELOPMENT ONLY
# =============================================================================

# API proxy for local development (to avoid CORS)
# VITE_API_PROXY=http://localhost:3001

# Mock delay (milliseconds) for simulating network latency
# VITE_MOCK_DELAY=500

# =============================================================================
# BACKEND API CONFIGURATION
# =============================================================================

# Server Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Database Configuration
DATABASE_URL=postgresql://localhost:5432/ucc_intelligence
DB_MAX_CONNECTIONS=20

# CORS Configuration (comma-separated origins)
CORS_ORIGIN=http://localhost:5173,http://localhost:5000

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Auth0 Configuration (for authentication in Phase 4)
AUTH0_DOMAIN=your-auth0-domain.auth0.com
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret
AUTH0_AUDIENCE=https://api.ucc-intelligence.com

# Redis Configuration (for job queue in Phase 3)
REDIS_URL=redis://localhost:6379

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# AWS Configuration (for secrets management in Phase 4)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key

# External API Keys (for data enrichment in Phase 2)
SEC_EDGAR_API_KEY=
OSHA_API_KEY=
USPTO_API_KEY=
CENSUS_API_KEY=
SAM_GOV_API_KEY=

# Commercial API Keys (optional - for Phase 2)
DNB_API_KEY=
CLEARBIT_API_KEY=
ZOOMINFO_API_KEY=

# Monitoring (for Phase 5)
SENTRY_DSN=
PROMETHEUS_PORT=9090

# Feature Flags (backend)
ENABLE_LOGGING=true
ENABLE_METRICS=true
ENABLE_RATE_LIMITING=true

# =============================================================================
# PRODUCTION DEPLOYMENT — REQUIRED SECRETS  (issue #235)
# =============================================================================
# When NODE_ENV=production, validateConfig() (server/config/index.ts) THROWS on
# boot if any of the following are missing — the server will not start. This is
# intentional (fail-closed). See docs/DEPLOYMENT.md for the full checklist.

# Always required (every environment):
#   JWT_SECRET                 — set above. No insecure fallback exists anymore.

# Required in production only:
#   DATABASE_URL               — set above (Postgres connection string).
#   CORS_ORIGIN                — set above. Must be non-empty; cannot be '*'
#                                (or empty) when credentials are enabled.

# Webhook signature secrets — verifiers FAIL CLOSED (HTTP 401) when unset, so
# production boot requires all four. Source each from its provider dashboard.
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_signing_secret
TWILIO_AUTH_TOKEN=your_twilio_auth_token
PLAID_WEBHOOK_SECRET=your_plaid_webhook_secret
SENDGRID_WEBHOOK_VERIFICATION_KEY=your_sendgrid_event_webhook_public_key

# Multi-tenant auth (issue #235): the org is derived from a JWT claim, NOT from
# request headers. Auth0 (or your IdP) MUST issue an `org_id` claim on access
# tokens, or /api/deals and /api/contacts return 403 for every user.
# JWT_ORG_CLAIM accepts an unprefixed name and also matches namespaced custom
# claims ending in /org_id or /orgId (e.g. https://<app>/org_id). Default: org_id
JWT_ORG_CLAIM=org_id
# Optional tier claim → data-tier entitlement (the x-data-tier header is ignored).
# JWT_TIER_CLAIM=tier
# Optional JWT verification hardening (recommended in production):
# JWT_ISSUER=https://your-auth0-domain.auth0.com/
# JWT_AUDIENCE=https://api.ucc-intelligence.com

# Payment + messaging provider keys (set when those integrations are live):
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_PRICE_STARTER=price_...
# STRIPE_PRICE_PROFESSIONAL=price_...
# STRIPE_PRICE_ENTERPRISE=price_...
# TWILIO_ACCOUNT_SID=AC...
# TWILIO_PHONE_NUMBER=+1...
# SENDGRID_API_KEY=SG....
# SENDGRID_FROM_EMAIL=alerts@your-domain.com
# PLAID_CLIENT_ID=...
# PLAID_SECRET=...
# PLAID_ENV=sandbox

# /api/metrics accepts a JWT OR this bearer token (for Prometheus scrapers):
# METRICS_TOKEN=your_metrics_scrape_token

# RLS NOTE: run the app under a NON-OWNER Postgres role so row-level security
# (migration 018) is enforced — a table owner bypasses non-FORCEd policies.

# =============================================================================
# NOTES
# =============================================================================
#
# 1. Never commit the actual .env file to version control
# 2. Rotate API keys regularly
# 3. Use different keys for development and production
# 4. Store production keys in a secure secrets manager
# 5. For GitHub Spark deployment, use GitHub Secrets for sensitive values
#
# For more information, see:
# - docs/DEPLOYMENT.md - Deploy prerequisites, release discipline, smoke test
# - DATA_PIPELINE.md - Detailed pipeline documentation
# - README.md - General project documentation
# - SECURITY.md - Security best practices
# - docs/tasks/PHASE_3_TASKS.md - Backend infrastructure setup
#
